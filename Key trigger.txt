javascript:(function(){const chatContainer=document.querySelector('div[style="min-width: 100%; display: table;"]');if(chatContainer){console.log("Chat container found. Messages are being monitored...");const observer=new MutationObserver((mutationsList)=>{for(const mutation of mutationsList){for(const addedNode of mutation.addedNodes){if(addedNode.nodeType===1&&addedNode.classList.contains("border-shade-lower")){const textSpan=addedNode.querySelector("span.text-primary");if(textSpan){const text=textSpan.innerText.trim();console.log("New message:",text);const socket=new WebSocket("ws://localhost:3000");socket.addEventListener("open",()=>{console.log("WS connection established");triggerQ(socket);});function triggerQ(sock){sock.send(text);}}}}}});observer.observe(chatContainer,{childList:true});}else{console.log("Chat container not found. The page may not have loaded completely.");}})();